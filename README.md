# Telegram Reminder Bot

Telegram-бот для создания напоминаний с кастомным расписанием в личных чатах и группах.

## Возможности

Бот поддерживает:

* одноразовые напоминания;
* повторы каждые N дней;
* повторы каждые N недель в выбранный день недели;
* повторы в N-й день недели месяца;
* кастомную дату старта для повторяющихся напоминаний;
* просмотр активных напоминаний;
* удаление активных напоминаний;
* сохранение напоминаний в SQLite;
* восстановление активных напоминаний после перезапуска.

## Команды

### Одноразовое напоминание

```text
/remind ГГГГ-ММ-ДД ЧЧ:ММ Текст
```

Пример:

```text
/remind 2026-06-10 12:12 Проверить статус релиза
```

### Повтор каждые N дней

С ближайшего подходящего времени:

```text
/every_days N ЧЧ:ММ Текст
```

Примеры:

```text
/every_days 1 12:12 Каждый день
/every_days 3 12:12 Каждые три дня
```

С кастомной даты:

```text
/every_days_from N ГГГГ-ММ-ДД ЧЧ:ММ Текст
```

Пример:

```text
/every_days_from 3 2026-06-10 12:12 Каждые три дня с 10 июня
```

### Повтор каждые N недель

С ближайшего подходящего дня недели:

```text
/every_week N ДЕНЬ ЧЧ:ММ Текст
```

Примеры:

```text
/every_week 1 SAT 12:12 Каждую субботу
/every_week 2 SUN 12:12 Каждое второе воскресенье
```

С кастомной даты:

```text
/every_week_from N ДЕНЬ ГГГГ-ММ-ДД ЧЧ:ММ Текст
```

Пример:

```text
/every_week_from 2 SUN 2026-06-14 12:12 Каждое второе воскресенье
```

### Повтор в N-й день недели месяца

С ближайшего подходящего дня:

```text
/monthly_weekday N ДЕНЬ ЧЧ:ММ Текст
```

Пример:

```text
/monthly_weekday 1 MON 12:12 Каждый первый понедельник месяца
```

С кастомной даты:

```text
/monthly_weekday_from N ДЕНЬ ГГГГ-ММ-ДД ЧЧ:ММ Текст
```

Пример:

```text
/monthly_weekday_from 1 MON 2026-07-01 12:12 Первый понедельник месяца с июля
```

### Список активных напоминаний

```text
/list
```

### Удаление напоминания

```text
/delete ID
```

Пример:

```text
/delete 1
```

## Дни недели

Используются английские сокращения:

```text
MON, TUE, WED, THU, FRI, SAT, SUN
```

## Локальный запуск

### 1. Создать виртуальное окружение

```powershell
python -m venv .venv
.venv\Scripts\activate
```

### 2. Установить зависимости

```powershell
pip install -r requirements.txt
```

### 3. Создать `.env`

В корне проекта нужно создать файл `.env`:

```env
BOT_TOKEN=your_telegram_bot_token
```

Токен можно получить у Telegram-бота `@BotFather`.

### 4. Запустить бота

```powershell
python bot.py
```

## Локальные файлы

В репозиторий не добавляются:

* `.env` — содержит токен бота;
* `.venv/` — локальное виртуальное окружение;
* `reminders.db` — локальная SQLite-база;
* `__pycache__/` — служебные файлы Python.

## Текущий статус

Проект находится на стадии MVP. Основная логика создания, хранения, восстановления, просмотра и удаления напоминаний уже реализована.
